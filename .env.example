# Database Configuration
DB_USER=postgres
DB_PASSWORD=your_password_here

# 🐳 Pour Docker, utilisez "db" au lieu de "localhost"
# DB_HOST=localhost      # ← Pour exécution locale sans Docker
DB_HOST=db               # ← Pour Docker Compose
DB_PORT=5432
DB_NAME=gamer_2025

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (pour production, séparer par des virgules)
# Exemple: ALLOWED_ORIGINS=https://votredomaine.com,https://www.votredomaine.com
ALLOWED_ORIGINS=http://localhost:3000

# Redis Configuration (optionnel, pour le cache)
# REDIS_URL=redis://localhost:6379     # ← Pour exécution locale sans Docker
REDIS_URL=redis://redis:6379           # ← Pour Docker Compose
